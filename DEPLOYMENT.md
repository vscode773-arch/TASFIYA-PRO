# 🚀 دليل النشر على Render.com

## الخطوة 1️⃣: إنشاء حساب على Render
1. اذهب إلى [render.com](https://render.com)
2. سجل الدخول باستخدام حساب GitHub الخاص بك

## الخطوة 2️⃣: نشر التطبيق
1. من لوحة التحكم في Render، اضغط على **"New +"** → **"Blueprint"**
2. اختر المستودع: `TASFIYA-PRO`
3. Render سيكتشف ملف `render.yaml` تلقائياً
4. اضغط **"Apply"**
5. انتظر حتى يكتمل البناء (5-10 دقائق)

## الخطوة 3️⃣: الحصول على رابط الموقع
بعد اكتمال النشر:
- سيظهر رابط الموقع مثل: `https://casher-cloud-dashboard.onrender.com`
- انسخ هذا الرابط

## الخطوة 4️⃣: ربط التطبيق المكتبي
1. افتح ملف: `src/modules/cloud-sync.js`
2. استبدل السطر:
   ```javascript
   this.CLOUD_URL = 'https://YOUR_RENDER_APP_URL.onrender.com';
   ```
   بـ:
   ```javascript
   this.CLOUD_URL = 'https://casher-cloud-dashboard.onrender.com'; // ضع رابطك هنا
   ```
3. **مفتاح المزامنة (API Key)** المستخدم حالياً: `CASHER_SYNC_2025`
   (⚠️ إذا أردت تغييره، عدله في ملف `render.yaml` ثم ارفع التحديث)

## الخطوة 5️⃣: اختبار المزامنة
1. أعد تشغيل تطبيق سطح المكتب
2. افتح Console (DevTools) وابحث عن:
   - `☁️ [Cloud Sync] Starting synchronization...`
   - `✅ [Cloud Sync] Sync Completed Successfully`

## الخطوة 6️⃣: الدخول إلى لوحة التحكم السحابية
1. اذهب إلى رابط موقعك: `https://casher-cloud-dashboard.onrender.com`
2. سجل الدخول نفس بيانات التطبيق المكتبي

---

## 📝 ملاحظات مهمة
- الموقع يعمل على **Render Free Plan** (مجاني)
- قد يدخل في "وضع السكون" بعد 15 دقيقة من عدم النشاط
- عند أول زيارة بعد السكون، قد يستغرق 30-60 ثانية للتشغيل
- المزامنة تتم كل **5 دقائق** تلقائياً من التطبيق المكتبي

## 🔐 الأمان
- استخدم `SYNC_API_KEY` قوي ومعقد في الإنتاج
- غير كلمات المرور الافتراضية بعد أول تسجيل دخول
- احتفظ بالمستودع **Private** على GitHub

---

🎉 **الآن يمكنك مشاهدة تقاريرك من أي مكان في العالم!**
